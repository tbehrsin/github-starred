version: 2.1

orbs:
  welcome: circleci/node@1.0.1

jobs:
  build-and-test:
    executor:
      name: node-orb/node
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run:
                command: yarn
      - node/with-splitting:
          steps:
            - run: env
            - run:
                command: yarn test
                when: always
      - run: env

workflows:
  build-and-test:
    jobs:
      - build-and-test
